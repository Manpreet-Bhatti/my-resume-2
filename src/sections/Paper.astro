---
interface Props {
  class?: string;
}
const { class: className = "" } = Astro.props;
---

<div
  class={`box-border h-264 w-204 bg-page p-5 shadow-[0_0_1rem_rgba(0,0,0,0.1)] transition-[background-color] duration-200 dark:bg-dark-page ${className}`}
>
  <slot />
</div>
